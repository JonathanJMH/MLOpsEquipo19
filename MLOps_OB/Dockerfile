# Utilizar una imagen base de PythonFROM python:3.9-slim# Establecer el directorio de trabajoWORKDIR /app# Copiar los archivos de la aplicación al contenedorCOPY . /app# Instalar las dependencias desde requirements.txtRUN pip install --no-cache-dir -r requirements.txt# Exponer puertos para Streamlit y FastAPIEXPOSE 8501 8000# Comando por defecto que usará Docker Compose para arrancar ambos serviciosCMD ["sh", "-c", "uvicorn fastapi_app:app --host 0.0.0.0 --port 8000 & streamlit run Home.py --server.port=8501 --server.enableCORS=false"]